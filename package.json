{
  "name": "df-map-timers",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "scripts": {
    "clean": "rimraf dist && rimraf .parcel-cache",
    "start": "npm run clean && cross-env TEMP=E:\\temp parcel",
    "build": "npm run clean && cross-env TEMP=E:\\temp parcel build",
    "deploy": "npm run build && gh-pages -d dist --branch publish --cname dftimers.corke.dev"
  },
  "dependencies": {
    "classnames": "^2.5.1",
    "date-fns": "^4.1.0",
    "immutability-helper": "^3.1.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "rimraf": "^6.0.1",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.14.4",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0",
    "parcel": "^2.14.0",
    "sharp": "^0.33.5"
  }
}